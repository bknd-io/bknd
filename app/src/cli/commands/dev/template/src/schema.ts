import { em, entity, text, boolean, number, datetime, json, jsonSchema, enumm } from "bknd";

export default em({
   // todos: entity("todos", {
   //    title: text(),
   //    done: boolean(),
   // }),
});
